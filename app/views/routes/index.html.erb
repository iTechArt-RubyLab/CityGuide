<p id="notice"><%= notice %></p>

<h1>Routes</h1>

<table>
  <thead>
    <tr>
      <th>Tour agency</th>
      <th>Visitor</th>
      <th>Start date</th>
      <th>Start end</th>
      <th>Hotel</th>
      <th>Cost</th>
      <th>Place</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @routes.each do |route| %>
      <tr>
        <td><%= route.tour_agency_id %></td>
        <td><%= route.visitor_id %></td>
        <td><%= route.start_date %></td>
        <td><%= route.start_end %></td>
        <td><%= route.hotel_id %></td>
        <td><%= route.cost %></td>
        <td><%= route.place_id %></td>
        <td><%= link_to 'Show', route %></td>
        <td><%= link_to 'Edit', edit_route_path(route) %></td>
        <td><%= link_to 'Destroy', route, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Route', new_route_path %>
